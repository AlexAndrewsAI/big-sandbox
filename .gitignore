# Don't commit persist except for specific files
/persist/*
!/persist/README.md


# Don't commit actual config, use config.example.yml as template
/config.yml

# Don't commit actual compose file, use docker-compose.example.yml as template
/docker-compose.yml

temp.*
*.temp
