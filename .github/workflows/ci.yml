name: CI

on:
  push:
    branches: [main, master, "feature/**"]
  pull_request:
    branches: [main, master]

jobs:
  shellcheck:
    name: Shell script lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get update && sudo apt-get install -y shellcheck

      - name: Shellcheck scripts
        run: shellcheck scripts/*.sh

  validate-config:
    name: Validate config.example.yml
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install yq
        run: |
          sudo curl -fsSL "https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64" -o /usr/local/bin/yq
          sudo chmod +x /usr/local/bin/yq

      - name: Lint config.example.yml
        run: |
          echo "Checking YAML syntax..."
          yq '.' config.example.yml > /dev/null

          echo "Checking apt list is non-empty..."
          apt_count=$(yq '.apt | length' config.example.yml)
          if [ "$apt_count" -eq 0 ]; then
            echo "ERROR: no apt packages found" >&2
            exit 1
          fi
          echo "Apt packages: $apt_count"

          echo "Checking install keys are valid..."
          count=$(yq '.install | length' config.example.yml)
          echo "Install entries: $count"

          for i in $(seq 0 $((count - 1))); do
            key=$(yq ".install | keys[$i]" config.example.yml)
            cmd=$(yq ".install.$key" config.example.yml)
            if [ -z "$cmd" ] || [ "$cmd" = "null" ]; then
              echo "ERROR: empty command for $key" >&2
              exit 1
            fi
            echo "  $key: OK"
          done

  validate-scripts:
    name: Validate shell scripts (syntax + dry-run)
    runs-on: ubuntu-latest
    needs: [shellcheck, validate-config]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install yq
        run: |
          sudo curl -fsSL "https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64" -o /usr/local/bin/yq
          sudo chmod +x /usr/local/bin/yq

      - name: Syntax check all scripts
        run: |
          bash -n scripts/build.sh
          bash -n scripts/run.sh
          bash -n scripts/installer.sh
          bash -n scripts/start-vnc.sh

      - name: Copy example configs
        run: cp config.example.yml config.yml

      - name: Dry-run installer against config
        run: |
          # Replace all install commands with 'true' so we validate parsing
          # and control flow without actually installing anything.
          yq '.install | with_entries(.value = "true")' config.yml > /tmp/dry-config.yml
          bash scripts/installer.sh /tmp/dry-config.yml

  docker-build:
    name: Build Docker image
    runs-on: ubuntu-latest
    needs: validate-scripts
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copy example configs
        run: |
          cp config.example.yml config.yml
          cp docker-compose.example.yml docker-compose.yml

      - name: Build sandbox image
        run: docker compose build --progress=plain
